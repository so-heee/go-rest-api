post:
  tags:
    - users
  summary: Create a new User.
  description: ユーザー情報登録
  operationId: PostUser
  parameters: []
  requestBody:
    description: user to create
    content:
      application/json:
        schema:
          $ref: ../../components/schemas/UserPostRequest.yml
        example:
          name: John Doe
          mail: john@gmail.com
          password: tSNP2amF
  responses:
    "201":
      description: CREATED
